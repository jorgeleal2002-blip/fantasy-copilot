# The shared draft room's database. Leave it out and the room feature is simply
# absent — the app keeps the solo mock and the same-board invite.
# See README, "Drafting together", for the two minutes of setup.
VITE_RTDB_URL=
